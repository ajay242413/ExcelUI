'use strict';

describe('myApp.display module', function() {

  beforeEach(module('myApp.display'));

  describe('display controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var displayCtrl = $controller('displayCtrl');
      expect(displayCtrl).toBeDefined();
    }));

  });
});
